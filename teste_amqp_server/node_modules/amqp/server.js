/*var express = require('express'),
http 		= require('http'),
path 		= require('path'),
amqp 		= require('amqp');*/

var amqp 	= require('amqp');

/*var app = express();

http.createServer(app).listen(app.get('port'), function(){
  console.log("RabbitMQ + Node.js app running on AppFog!");
});*/


//Vamos precisar dessas configurações
/*app.configure(function(){
	  app.set('port', process.env.PORT || 3000);
	  app.set('views', __dirname + '/views');
	  app.set('view engine', 'jade');
	  app.use(express.bodyParser());
	  app.use(express.static(path.join(__dirname, 'public')));
});*/

//Definindo o padrão da conexão para a troca de mensagens e para minha fila de mensagens
//Mais tarde, esses valores serão substituídas quando nossa conexão RabbitMQ estiver configurado.
/*app.connectionStatus = 'No server connection';
app.exchangeStatus = 'No exchange established';
app.queueStatus = 'No queue established';*/